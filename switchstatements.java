public class switchstatements { //this took me the longest since this is an entirely new concept to me, anyway this switch statement literally just
                                //just detects what "gender" something is with variables (no inputs however so basically everything  has to be changed manually)
    public static void main(String[] args) {
        String gender = "MALE";
        if (gender.equals("FEMALE")) {

        } else if (gender.equals("MALE")) {

        } else if (gender.equals("PERFER_NOT_TO_SAY")){

        } else {
            switch (gender) {
                case "FEMALE":
                    break;
                case "MALE":
                    break;
                case "PERFER_NOT_TO_SAY":
                    break;
                default:
         

            }


        }

        }


    }   